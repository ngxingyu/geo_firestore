name: geo_firestore_flutter
description: GeoFirestore for Flutter to do location based queries with Firestore
homepage: https://github.com/percula/geo_firestore
version: 0.0.2

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cloud_firestore: ^0.13.6

dev_dependencies:
  flutter_test:
    sdk: flutter